# build output
dist/
.astro/

# dependencies — kept out of iCloud sync via the .nosync trick (see README).
# We commit neither the synced shape nor the renamed one.
node_modules/
node_modules.nosync/

# generated by astro
.DS_Store
Thumbs.db
*.log

# environment
.env
.env.local
.env.production

# Vercel adapter output — built fresh by Vercel on every deploy
.vercel/
